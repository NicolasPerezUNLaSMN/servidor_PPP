import 'package:flutter/material.dart';

class CrearObraPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Arbolada'),
        centerTitle: true,
      ),
      body: Text('Sin implementar'),
    );
  }
}